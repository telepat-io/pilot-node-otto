# Secrets — the ed25519 publisher signing key lives here. NEVER commit it.
/secure/

# Build outputs (regenerated by scripts/build-all.sh)
/build/
/bundles/
/dist/

# Local deploy env (copy from .env.example)
.env

# Node
node_modules/
app/node_modules/
app/bin/

# Extension build workspace (the unpacked artifact under extension-dist/ IS committed)
extension-dist/node_modules/
extension-dist/.output/
extension-dist/.wxt/
